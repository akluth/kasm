# project_hello

This is a minimal KASM project-mode example with two source files.

Build and run it from this directory:

```sh
../../kasm build --verbose
./build/project_hello
```

To assemble only the object files:

```sh
../../kasm build --no-link
```
