; Tiny string helper constants/placeholders.
;
; v0.8 intentionally avoids adding hidden runtime routines. A future version may
; provide kasm_strlen once byte loads/comparisons are part of the core subset.
define KASM_STRING_ROUTINES 0
